# Shared contracts

Contains the smallest stable set of models, protocols, configuration, and errors required across modules.

Changes require peer review from another module owner and corresponding contract tests.
